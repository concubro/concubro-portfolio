# concubro-portfolio

A simple static HTML portfolio site for concubro.

To add a new article: copy [_article-template.html](./_article-template.html) to a new top-level `.html` file, point it at a new `.md` file, add the link to `index.html`, add the URL to `sitemap.xml`, and generate a matching `og-*.png` social card.
